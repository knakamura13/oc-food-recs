node_modules
.claude/worktrees/
__oc-food-recs-reddit-thread-01-parsed/
__pycache__/

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/dist
/playwright-report
/test-results

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# scratch HTML drop zone for ad-hoc ingests
data/new.html

# staging area for scraped HTML awaiting ingest (raw, not committed)
data/uningested-threads/*
!data/uningested-threads/.gitkeep

# archived HTML exports moved here after a successful ingest (derived, not committed)
data/threads/*.html

# local geocode cache (derived from Nominatim; delete to force a re-geocode)
data/geocode-cache.json

# local DB backups + input snapshots (may contain data; never commit)
data/backups/
